# 2026-07-14

## 修复 voxcpm2-gradio.ts 双引号重复转义
- 文件 `src/audio_create/voxcpm2-gradio.ts` 全文字符串引号被重复转义（`""xxx""` → 应为 `"xxx"`），共 50+ 处
- 全局替换 `""` → `"` 修复
- Line 112 顺便将冗余的 `"" + audioPath` 改为有意义的 `"File not found: " + audioPath`
- `tsc --noEmit` 验证通过，src/ 零错误

## roleMap 架构重构：RoleEntry.speaker → defaults
- `RoleEntry`: `speaker: string` → `defaults: Record<string, unknown>`，角色可携带任意 provider 配置（不只 speaker）
- `ResolvedRole`: 移除 `speaker`，新增 `options: Record<string, unknown>`（合并后的最终配置）
- `resolveRole()` 重写：合并层 `roleEntry.defaults → voiceType 快捷覆盖 → item.options`（item 最高优先）
- 所有 4 个 provider config 文件的 default roleMap 转换完成（sed 批量处理）
- 4 个 provider + batch/run.ts 全部改用 `resolved.options`，不再手动拼 `...item.options`
- `tsc --noEmit` src/ 零错误

## 文档重构：README.md 精简 + CRATE_JSON_SKILL.md 新增 voxcpm2_gradio
- README.md 从 354 行精简到 102 行，去掉与 CRATE_JSON_SKILL.md 重复的 JSON 格式、provider 特性、静态工具等详细内容
- README 保留：简介、provider 表（新增 voxcpm2_gradio）、快速开始、CLI 参数表、项目结构、辅助工具
- CRATE_JSON_SKILL.md 新增模式 D：VoxCPM2 Gradio（voxcpm2_gradio），含三种合成模式（声音设计/纯音频克隆/极致克隆）、JSON 示例、字段说明、配置参数、.env 配置
- 混合模式从 D 调整为 E
- 差异对照表新增 voxcpm2_gradio 列
- AI Prompt 模板新增 voxcpm2_gradio 条目
